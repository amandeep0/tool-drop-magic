const ProBadge = () => {
  return (
    <div className="inline-flex items-center px-4 py-1.5 border-2 border-accent rounded-full transition-colors hover:bg-accent/10">
      <span className="text-sm font-bold tracking-wide text-accent">Pro Plan</span>
    </div>
  );
};

export default ProBadge;
